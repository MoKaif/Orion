{
  "//": "No API keys here. Cloud keys load from .env / config/secrets.json (gitignored).",

  "routing": {
    "//": "Maps cognitive mode -> preferred provider. Chat runs on DeepSeek v4-flash (the",
    "//2": "cheapest cloud brain, user's call 2026-07-22); background dispatch/compress stay",
    "//3": "on Ollama. Anthropic remains wired but disabled (account has no credits).",
    "reflex":    "deepseek",
    "embed":     "fastembed",
    "reasoning": "deepseek",
    "deep_work": "deepseek",
    "dispatch":  "ollama",
    "compress":  "ollama"
  },

  "escalation": {
    "//": "If the resolved provider is unavailable pre-token, escalate down this chain.",
    "//2": "deepseek (cheap cloud brain) -> gemini (future) -> ollama (local, always there).",
    "local_first": true,
    "confidence_threshold": 0.6,
    "fallback_chain": ["deepseek", "gemini", "ollama"]
  },

  "budget": {
    "//": "Hard daily ceilings enforced by the router.",
    "daily_token_ceiling": 2000000,
    "warn_at_fraction": 0.8
  },

  "pricing": {
    "//": "USD per 1M tokens, per model. cache_read ~= 0.1x input, cache_write ~= 1.25x input.",
    "//2": "Locals are free. The router costs each turn against the model that actually ran.",
    "deepseek-v4-flash":{ "input": 0.14, "output": 0.28, "cache_read": 0.0028, "cache_write": 0.0 },
    "deepseek-v4-pro":  { "input": 0.435, "output": 0.87, "cache_read": 0.003625, "cache_write": 0.0 },
    "claude-opus-4-8":  { "input": 5.0, "output": 25.0, "cache_read": 0.5,  "cache_write": 6.25 },
    "claude-sonnet-5":  { "input": 3.0, "output": 15.0, "cache_read": 0.3,  "cache_write": 3.75 },
    "claude-haiku-4-5": { "input": 1.0, "output": 5.0,  "cache_read": 0.1,  "cache_write": 1.25 },
    "gemini-2.5-flash": { "input": 0.3, "output": 2.5,  "cache_read": 0.075, "cache_write": 0.3833 },
    "qwen2.5:3b":       { "input": 0.0, "output": 0.0 }
  },

  "providers": {
    "ollama": {
      "enabled": true,
      "base_url": "http://localhost:11434",
      "model": "qwen2.5:3b",
      "context_window": 4096
    },
    "fastembed": {
      "enabled": true,
      "model": "BAAI/bge-small-en-v1.5",
      "dim": 384
    },
    "anthropic": {
      "//": "The API-based main orchestrator. `model` is the default; `mode_models` tiers",
      "//2": "per cognitive mode so deep_work gets Opus and lighter reasoning gets Sonnet.",
      "//3": "effort is low|medium|high|xhigh|max. Costs are tracked per model (see pricing).",
      "enabled": false,
      "model": "claude-opus-4-8",
      "mode_models": {
        "reflex":    "claude-haiku-4-5",
        "reasoning": "claude-sonnet-5",
        "deep_work": "claude-opus-4-8"
      },
      "max_tokens": 4096,
      "effort": "medium",
      "api_key_env": "ANTHROPIC_API_KEY"
    },
    "deepseek": {
      "//": "The cheap cloud brain: OpenAI-compatible REST, automatic server-side context",
      "//2": "caching (usage splits cache hits/misses; priced per the table above).",
      "//3": "v4-flash is the cheapest model; v4-pro is the pricier sibling if ever needed.",
      "enabled": true,
      "model": "deepseek-v4-flash",
      "base_url": "https://api.deepseek.com",
      "max_tokens": 4096,
      "api_key_env": "DEEPSEEK_API_KEY"
    },
    "gemini": {
      "//": "Future cheap-cloud fallback. Disabled until keyed.",
      "enabled": false,
      "model": "gemini-2.5-flash",
      "api_key_env": "GEMINI_API_KEY",
      "rate_limits": { "rpm": 15, "rpd": 1500 }
    }
  }
}
